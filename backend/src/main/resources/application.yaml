spring:
  application:
    name: backend

  # --- DATABASE CONFIGURATION ---
  # SQL Server Configuration (Active)
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=NURSING_HOME_DB;encrypt=false;trustServerCertificate=true
    username: sa
    password: MatKhauCuaBan123!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect

  # Commented out H2 database configuration for local fallback:
  # datasource:
  #   url: jdbc:h2:mem:eldercare;DB_CLOSE_DELAY=-1
  #   username: sa
  #   password: password
  #   driver-class-name: org.h2.Driver
  # jpa:
  #   database-platform: org.hibernate.dialect.H2Dialect
  #   hibernate:
  #     ddl-auto: update
  #   show-sql: true
