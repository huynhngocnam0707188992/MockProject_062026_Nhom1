spring:
  datasource:
    url: jdbc:h2:mem:eldercare;DB_CLOSE_DELAY=-1;MODE=MSSQLServer
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  cors:
    allowed-origins: http://localhost:5173
